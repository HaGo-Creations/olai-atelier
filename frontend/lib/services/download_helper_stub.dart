// lib/services/download_helper_stub.dart
//
// Non-web stub — does nothing (this is a web-only demo).

void triggerDownload(String url, {String? suggestedFilename}) {
  // No-op on non-web platforms
}
